# Config Server 連線設定
# 如果使用 Spring Cloud Config Server，可在此設定

spring:
  application:
    name: customers-service

# 如需連接 Config Server，請取消下面的註解
# spring:
#   cloud:
#     config:
#       uri: http://localhost:8888
#       fail-fast: true
